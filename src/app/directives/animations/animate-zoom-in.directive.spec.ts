import { AnimateZoomInDirective } from './animate-zoom-in.directive';

describe('AnimateZoomInDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateZoomInDirective();
    expect(directive).toBeTruthy();
  });
});
